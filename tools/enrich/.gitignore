# Secrets — never commit
.env

# Runtime state — regenerated on each run
state/last-snapshot.json
state/last-snapshot.json.bak
state/*.log

# Python noise
__pycache__/
*.pyc
.venv/
venv/

# OS noise
.DS_Store
